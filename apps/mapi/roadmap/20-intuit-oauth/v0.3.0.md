# v0.3.0 — Schema clients + intuit_tokens + endpoints OAuth + proxy V3

**Estado**: 📅 Pendiente (esperando aprobación del TDD)
**Inicio**: TBD
**Cierre estimado**: TBD
**Módulo**: 20-intuit-oauth
**TDD ref**: [README.md](README.md)

---

## Alcance

### Sí entra

- Schema `clients` (mínimo, heredado mapi v0.x).
- Schema `intuit_tokens` (heredado mapi v0.x).
- Migration que crea ambas tablas + trigger `clients.updated_at`.
- `EncryptionService` AES-256-GCM con `ENCRYPTION_KEY`.
- `IntuitOauthClientFactory` (genera SDK por cliente, fix shape completo).
- `IntuitTokensService` (get/refresh transparente).
- `IntuitApiService` (proxy genérico con retry-on-401).
- `IntuitOauthService` (orquesta OAuth flow + Redis state).
- 6 endpoints `/v1/intuit/*` y `/v1/clients/:id/connect`.
- 6 errores de dominio mapeados.
- 7 tipos de eventos `event_log`.
- 2 métricas Prometheus.
- Tests Tipo A: CR-intuit-001 a 041.
- Tests Tipo B: SMK-intuit-001 a 004.

### NO entra

- **Migración 77 clientes desde mapi v0.x** — vive en v0.3.1.
- Bridge WebSocket, connectors, mappers, staging — otros módulos.

---

## Eventos a agregar (event_log)

7 eventos. Detalle en [README.md sección "Eventos event_log"](README.md#eventos-event_log).

- [ ] `intuit.client.created`
- [ ] `intuit.client.reauth_silent`
- [ ] `intuit.client.reauth_target`
- [ ] `intuit.tokens.refreshed`
- [ ] `intuit.tokens.refresh_failed`
- [ ] `intuit.tokens.deleted`
- [ ] `intuit.api.call_failed`

---

## Errores de dominio nuevos

6 errores. Detalle en [README.md sección "Errores de dominio"](README.md#errores-de-dominio-mapping-en-domain-error-filter-ts).

- [ ] `IntuitTokensNotFoundError` (404, code `INTUIT_TOKENS_NOT_FOUND`)
- [ ] `IntuitRefreshTokenExpiredError` (401, code `INTUIT_REFRESH_EXPIRED`)
- [ ] `IntuitAuthorizationError` (400, code `INTUIT_AUTH_ERROR`)
- [ ] `IntuitBadRequestError` (400, code `INTUIT_BAD_REQUEST`)
- [ ] `IntuitStateInvalidError` (400, code `INTUIT_STATE_INVALID`)
- [ ] `ClientNotFoundError` (404, code `CLIENT_NOT_FOUND`)

---

## Endpoints API nuevos

6 endpoints. Cada uno con `@ApiTags('Intuit OAuth')`, `@ApiOperation`, `@ApiResponse`, DTOs Zod.

- [ ] `POST /v1/intuit/connect` (`@Roles('admin')`).
- [ ] `POST /v1/clients/:id/connect` (`@Roles('admin')`).
- [ ] `GET /v1/intuit/callback` (`@Public()`, retorna HTML).
- [ ] `POST /v1/intuit/:realmId/call` (`@Roles('admin')`).
- [ ] `GET /v1/intuit/tokens` (`@Roles('admin')`).
- [ ] `DELETE /v1/intuit/tokens/:clientId` (`@Roles('admin')`).

---

## TODOs (orden TDD-first secuencial)

### Bloque 1 — Schema + migration + env vars

- [ ] Agregar deps a `apps/mapi/package.json`:
  - `intuit-oauth` (SDK oficial Intuit).
  - `axios` (para HTTP requests del proxy).
- [ ] Actualizar `core/config/config.schema.ts` con vars nuevas:
  - `ENCRYPTION_KEY` (string, regex base64 de 32 bytes).
  - `INTUIT_CLIENT_ID`, `INTUIT_CLIENT_SECRET` (string requeridos).
  - `INTUIT_REDIRECT_URI` (URL requerida).
  - `INTUIT_ENVIRONMENT` (enum `'production'`, default `'production'`).
  - `INTUIT_MINOR_VERSION` (number coerce, default 75).
- [ ] Actualizar `AppConfigService` con getters tipados.
- [ ] Actualizar `.env.example` y `.env.test.example`.
- [ ] Crear `src/db/schema/clients.ts` con shape heredado mapi v0.x.
- [ ] Crear `src/db/schema/intuit-tokens.ts` con shape heredado + `client_id` PK + interfaz `DecryptedIntuitToken`.
- [ ] Re-export en `src/db/schema/index.ts`.
- [ ] Generar migration `npm run db:generate` → renombrar a `0002_clients_intuit_tokens.sql` + agregar trigger `clients.updated_at`.
- [ ] Aplicar migration a `mapi_local`: `npm run db:migrate`.
- [ ] Commit: `feat(mapi): schema clients + intuit_tokens + migration (20-intuit-oauth bloque 1)`.

### Bloque 2 — EncryptionService + tests

- [ ] Crear `src/core/encryption/encryption.service.ts` con `encrypt(plain): string` y `decrypt(cipher): string`. AES-256-GCM con `crypto` nativo. Format: `<iv-base64>:<ciphertext-base64>:<tag-base64>`.
- [ ] Crear `src/core/encryption/encryption.module.ts` `@Global()`.
- [ ] Registrar en `app.module.ts`.
- [ ] Tests Tipo A `test/unit/intuit/encryption.service.spec.ts`:
  - CR-intuit-001 (encrypt/decrypt round-trip).
  - CR-intuit-002 (IV random produce ciphertext distinto).
  - CR-intuit-003 (decrypt con key incorrecta lanza).
  - CR-intuit-004 (decrypt con tag modificado lanza).
- [ ] Commit: `feat(mapi): EncryptionService AES-256-GCM + tests (bloque 2)`.

### Bloque 3 — IntuitOauthClientFactory

- [ ] Crear `src/modules/20-intuit-oauth/intuit-oauth-client.factory.ts`:
  - `create(): IntuitOauthClient` (instancia nueva).
  - `applyToken(client, decryptedToken)`: setea token con shape **completo** (createdAt, expires_in, x_refresh_token_expires_in) — fix D-mapi-v0.x-118.
- [ ] Test Tipo A: `applyToken` con shape parcial NO debe romper (defensive).
- [ ] Commit: `feat(mapi): IntuitOauthClientFactory + fix shape token (bloque 3)`.

### Bloque 4 — Errores + IntuitTokensService + IntuitTokensRepository

- [ ] Crear `src/modules/20-intuit-oauth/intuit-oauth.errors.ts` con las 6 clases extendiendo `DomainError`.
- [ ] Actualizar `domain-error.filter.ts` `STATUS_BY_CODE` con los 6 codes.
- [ ] Crear `src/modules/20-intuit-oauth/tokens/intuit-tokens.repository.ts` con CRUD + `findByRealmId`.
- [ ] Crear `src/modules/20-intuit-oauth/tokens/intuit-tokens.service.ts`:
  - `getValidTokens(clientId): Promise<DecryptedIntuitToken>`.
  - `refresh(clientId): Promise<DecryptedIntuitToken>` interno.
  - `saveTokens(clientId, realmId, plain)` → cifra + UPSERT.
  - `deleteTokens(clientId)`.
- [ ] Tests Tipo A `test/unit/intuit/intuit-tokens.service.spec.ts`:
  - CR-intuit-010 a 014.
  - Fixtures: `tokens-vigentes.json`, `tokens-por-expirar.json`, `tokens-refresh-expired.json`.
- [ ] Commit: `feat(mapi): errores + IntuitTokensService con refresh transparente (bloque 4)`.

### Bloque 5 — IntuitApiService (proxy genérico)

- [ ] Crear `src/modules/20-intuit-oauth/api-client/intuit-api.service.ts`:
  - `call({ realmId, method, path, body? }): Promise<unknown>`.
  - Resuelve clientId por realmId, llama `getValidTokens`, hace HTTP, retry-on-401.
  - Métricas Prometheus: `intuit_api_calls_total{path, status}`.
- [ ] Tests Tipo A:
  - CR-intuit-020 a 025.
  - Mock axios.
- [ ] Commit: `feat(mapi): IntuitApiService proxy genérico V3 + tests (bloque 5)`.

### Bloque 6 — IntuitOauthService (callback flow)

- [ ] Crear `src/modules/20-intuit-oauth/oauth/intuit-oauth.service.ts`:
  - `getAuthorizationUrlForNewClient(adminUserId)`.
  - `getAuthorizationUrl(adminUserId, targetClientId)`.
  - `handleCallback(code, realmId, state, fullUrl)`.
- [ ] Redis state: key `intuit:state:<state>`, TTL 600s.
- [ ] Crear `clients.repository.ts` mínimo (insert / findByRealmId / findById).
- [ ] Tests Tipo A:
  - CR-intuit-030 a 035.
- [ ] Commit: `feat(mapi): IntuitOauthService + Redis state + tests (bloque 6)`.

### Bloque 7 — Controllers + DTOs

- [ ] Crear DTOs Zod en `oauth/intuit-oauth.dto.ts`:
  - `AuthorizeResponseDto`.
  - `CallbackQueryDto`.
- [ ] Crear DTOs en `admin/intuit-admin.dto.ts`:
  - `IntuitCallRequestDto`.
  - `TokenStatusDto`.
- [ ] Crear `oauth/intuit-oauth.controller.ts`:
  - `POST /v1/intuit/connect`.
  - `POST /v1/clients/:id/connect`.
  - `GET /v1/intuit/callback` (`@Public()`, retorna HTML).
- [ ] Crear `admin/intuit-admin.controller.ts`:
  - `POST /v1/intuit/:realmId/call`.
  - `GET /v1/intuit/tokens`.
  - `DELETE /v1/intuit/tokens/:clientId`.
- [ ] Crear `intuit-oauth.module.ts` registrando controllers + services.
- [ ] Registrar `IntuitOauthModule` en `app.module.ts`.
- [ ] Anotar todos los endpoints con `@ApiTags`, `@ApiOperation`, `@ApiResponse` (DOC-1 a DOC-8).
- [ ] Commit: `feat(mapi): controllers /v1/intuit/* + DTOs (bloque 7)`.

### Bloque 8 — Cron de métricas

- [ ] Crear `tokens/intuit-tokens.metrics-cron.ts`:
  - `@Cron('0 0 * * * *')` (cada hora).
  - Lee tokens, calcula `daysUntilRefreshExpiry`, actualiza gauge Prometheus.
  - `onApplicationBootstrap`: corre una vez al arrancar.
- [ ] Test Tipo A: CR-intuit-040, 041.
- [ ] Commit: `feat(mapi): cron de métricas tokens + tests (bloque 8)`.

### Bloque 9 — Tests Tipo B (smoke con DB real)

- [ ] `test/e2e/intuit-oauth.e2e-spec.ts`:
  - SMK-intuit-001: flujo OAuth con SDK mockeado.
  - SMK-intuit-002: GET /v1/intuit/tokens lista status sin secretos.
  - SMK-intuit-003: DELETE /v1/intuit/tokens/:clientId.
  - SMK-intuit-004: cascade delete clients → intuit_tokens.
- [ ] Commit: `test(mapi): smoke E2E para 20-intuit-oauth (bloque 9)`.

### Bloque 10 — Cierre v0.3.0

- [ ] Verificar smoke test del módulo (sección abajo).
- [ ] Bumpear `apps/mapi/package.json` a `0.3.0`.
- [ ] Marcar este `v0.3.0.md` como ✅ con cierre date.
- [ ] Actualizar `apps/mapi/roadmap/README.md`: índice + cronológico + decisiones.
- [ ] **NO marcar el módulo `20-intuit-oauth` como ✅ aún** — la migración v0.3.1 falta.
- [ ] Commit: `release(mapi): v0.3.0 — schema clients + intuit_tokens + endpoints OAuth`.
- [ ] Tag `mapi-v0.3.0` + push.

---

## Decisiones tomadas durante esta versión

(Vacío al arrancar.)

---

## Fixes durante desarrollo

(Vacío al arrancar.)

---

## Notas operativas

- **Variables Coolify antes del push del cierre:** `ENCRYPTION_KEY` (mismo valor que mapi v0.x prod), `INTUIT_CLIENT_ID`, `INTUIT_CLIENT_SECRET` (de la app "BV CPAs, PLLC" production), `INTUIT_REDIRECT_URI=https://mapi.kodapp.com.mx/v1/intuit/callback`. Si faltan, el deploy falla por config Zod.
- **Importante:** la URL de callback en Intuit Developer Portal de la app "BV CPAs, PLLC" debe **agregar** `https://mapi.kodapp.com.mx/v1/intuit/callback` (sin remover la actual `https://mapi.alfredo.mx/...` que mapi v0.x sigue usando hasta apagar). Las apps Intuit aceptan múltiples redirect URIs.
- **Connect en local:** redirect_uri local apunta a `https://dev.alfredo.mx/v1/intuit/callback` (tunnel cloudflared), usar app "BV CPAs Dev". No usar credenciales de prod en local.
