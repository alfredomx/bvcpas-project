// Ejecutor de operaciones DOM genérico (kiro 22-dom-executor).
// Punto de entrada público del módulo.

export { executeDom } from './dom-executor'
export type { DomStep, DomOp, DomInstruction, DomStepResult, DomResult } from './types'
