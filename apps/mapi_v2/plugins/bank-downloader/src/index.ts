/**
 * Cara pública (barrel) del plugin Bank Downloader. El core lo importa por el
 * alias `@plugins/bank-downloader/src` y solo ve su `ModuleDef`.
 */
export { bankDownloaderPlugin } from './bank-download.plugin'
