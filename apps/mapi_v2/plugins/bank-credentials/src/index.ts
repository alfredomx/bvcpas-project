/**
 * Cara pública (barrel) del plugin Bank Credentials. El core lo importa por el
 * alias `@plugins/bank-credentials/src` y solo ve su `ModuleDef`.
 */
export { bankCredentialsPlugin } from './bank-credentials.plugin'
