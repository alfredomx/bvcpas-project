# BV CPAs - frontend Next.js. Vars públicas (NEXT_PUBLIC_*) van al bundle del browser.
# Vars sin prefijo NEXT_PUBLIC_ sólo están disponibles en server components / route handlers.

# URL base del backend mapi (sin `/v1` al final — los paths se arman con `/v1/...` en `src/lib/http.ts`).
#
# Dev (tunnel cloudflared): https://dev.alfredo.mx
# Prod:                     https://mapi.kodapp.com.mx
NEXT_PUBLIC_API_URL=https://dev.alfredo.mx
