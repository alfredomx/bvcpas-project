# ---------- apps/mcp (servidor MCP -> mapi) ----------
# A dónde apunta el wrapper. Local: mapi en :4000. Prod: https://mapi.kodapp.com.mx
MAPI_BASE_URL=http://localhost:4000
# JWT admin para Authorization: Bearer. Local: el mismo ADMIN_JWT_SECRET de apps/mapi/.env
MAPI_JWT=
